apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mediaserver-pvc
  namespace: mediaserver
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: standard
  resources:
    requests:
      storage: 5Gi
